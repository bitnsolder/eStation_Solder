#ifndef DEBUG_SYS_H
#define DEBUG_SYS_H

#include "lvgl/lvgl.h"
#include <stdio.h>
#include <stdarg.h>

// Chỉnh 1 để BẬT monitor (đo FPS/CPU/RAM)
// Chỉnh 0 để TẮT (khi xuất xưởng hoặc khi cần CPU rảnh nhất)
#define USE_SYS_MONITOR 1
//
//void USB_Log(char* msg);
//void Debug_Init(lv_disp_drv_t * disp_drv);
//void My_Log(const char* fmt, ...);
//void my_monitor_cb(lv_disp_drv_t * disp_drv, uint32_t time, uint32_t px);
//void USB_Printf(const char* fmt, ...);
//void my_monitor_cb(lv_disp_drv_t * disp_drv, uint32_t time, uint32_t px);
//void Heartbeat();

#endif
